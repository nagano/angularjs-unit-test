# angularjs-unit-test
Sample angularjs project to try unit tests
## Installation

#### Using Yarn
`yarn install`

`yarn run karma start`

#### Using NPM
`npm install`

`npm install -g karma`

`karma start`
